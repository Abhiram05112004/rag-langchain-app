# Copy this file to .env and add your actual API key
# Only the API key needs to be set - all other configurations are in the code

# Groq API Configuration (REQUIRED)
GROQ_API_KEY=your_groq_api_key_here
